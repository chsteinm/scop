#ifndef MODEL_HPP
#define MODEL_HPP

#include <glad/glad.h>
#include <iostream>

class Model
{
    public:

        // bool texActive = false;
        GLuint VAO = 0, VBO = 0, EBO = 0 ;
    
        Model();

        // void    init();

};

#endif // MODEL_HPP